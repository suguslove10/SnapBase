# Database
DB_USER=dbbackup
DB_PASSWORD=dbbackup
DB_NAME=dbbackup

# AES-256 encryption key — MUST be exactly 32 bytes
# Generate: openssl rand -hex 16 | tr -d '\n'  (gives 32 hex chars = 32 bytes)
ENCRYPTION_KEY=change-me-to-32-byte-secret-key!!

# JWT
JWT_SECRET=change-me-in-production

# MinIO / S3 storage
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=backups

# Email notifications (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=noreply@snapbase.local
NOTIFICATIONS_ENABLED=false

# OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=Ov23li8g0bnn3juxm4Ks
GITHUB_CLIENT_SECRET=2773c7f28afefd28a90c15aee0459da7d3c80c93
FRONTEND_URL=http://localhost:3000
